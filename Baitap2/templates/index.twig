{% block content %}
  <h1>List of Users</h1>
  <ul>
    {% for user in users %}
        <li>{{ user.id}}</li>
        <li>{{ user.name }}</li>
        <li>{{ user.email }}</li>
        <li>{{ user.password }}</li>
    {% endfor %}
  </ul>
{% endblock %}
